$(function() {
	var header = '<div class="header">';
	header += '<div class="logo"><img src="images/logo2.png" alt="圣康研究院" /></div>';
	header += '<div class="loginBox">';
	if (window.location.href.match("login.html") == null) {
		header += '<span class="login"><a href="login.html">登 录</a></span>';
		header += '<span class="register"><a href="register.html">注 册</a></span>';
	}
	header += '</div></div>';
	$("body").prepend(header);
});